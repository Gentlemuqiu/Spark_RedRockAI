// 是否开启单模块调试
val isDebug by extra { false }

// SDK版本
val app_sdk = mapOf(
    "compileSdk" to 34,
    "minSdk" to 24,
    "targetSdk" to 34
)

// 三方库版本
val versions = mapOf(
    "arouter_version" to "1.5.2",
    "rxjava_version" to "x.x.x"
)
